[build]
builder = "railpack"
cmd = "cd frontend && npm ci && npm run build && cd .. && cargo build --release && mkdir -p bin && cp target/release/round-robin-quorum bin/"

[deploy]
startCommand = "round-robin-quorum"
